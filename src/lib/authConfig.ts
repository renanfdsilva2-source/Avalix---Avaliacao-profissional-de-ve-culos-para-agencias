// Production URL used for all auth email redirects (signup confirmation,
// password recovery, etc.). Make sure this URL is added to the auth
// "Redirect URLs" allow list in the backend settings.
export const PROD_URL = "https://avalix.app.br";
